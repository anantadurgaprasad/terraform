apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: secretstore-secretmanager
spec:
  provider:
    aws:
      service: SecretsManager
      region: ${region}
      auth:
        jwt:
          serviceAccountRef:
            name: ${service_account_name}


